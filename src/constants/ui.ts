/** Shared font stacks used across all UI components. */
export const FONT_MONO = "'Space Mono', monospace" as const;
export const FONT_DISPLAY = "'Syne', sans-serif" as const;

/** Animation duration (ms) for the AnimatedNumber easing. */
export const ANIMATED_NUMBER_DURATION_MS = 800;

/** Gap (px) between grid cells in the visualizer. */
export const GRID_CELL_GAP = 3;

/** Stagger delay (ms) between each item reveal in the grid animation. */
export const REVEAL_STAGGER_MS = 180;

/** Initial delay (ms) before the first item reveal begins. */
export const REVEAL_INITIAL_DELAY_MS = 200;

